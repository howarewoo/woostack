---
title: What is woostack?
description: Evidence-driven workflows with selected build/plan persistence and a proved-fix issue path.
---

woostack is a model-agnostic collection of gated software-development skills for planning,
implementation, review, diagnostics, and reusable knowledge.

```bash
pnpx skills add howarewoo/woostack
```

- **Repository-first truth:** the user's approved workflow contract authorizes work; Git and GitHub
  prove source, branches, pull requests, reviews, and merge state.
- **Explicit durable plans:** caller-selected build/standalone-plan persistence uses one project,
  one parent plan issue, and one native child issue per increment. A proved new fix prompt binds or
  creates one compatible issue after root-cause proof and never creates a project hierarchy. Linear
  remains optional for non-fix workflows and for fix diagnosis before root-cause proof; a proved new
  fix requires the configured official MCP issue path before implementation.
- **Decision-maker/coder separation:** the generic
  [engineer-agent contract](/docs/concepts/engineer-agents) keeps scope, independent review, and
  acceptance separate from isolated implementation.
- **Non-authoritative diagnostics:** local reports are evidence, not permission or lifecycle state.
- **Agent- and model-agnostic:** the same skill logic supports
  [multiple harnesses](/docs/harnesses).
- **Stable adoption surface:** one install provides twenty-two public command/adoption skills at
  twenty-four fixed `SKILL.md` locations.
- **Pairs with [impeccable](https://github.com/pbakaus/impeccable):** woostack's recommended
  front-end design skill.

## Why woostack?

The collection keeps the whole delivery loop coherent without requiring a local development
ledger or a particular provider:

- one install and one routing surface;
- explicit approval gates and bounded task contracts;
- safe concurrency through declared dependencies and isolated worktrees;
- decision-maker/coder role separation;
- portable execution across supported harnesses;
- optional plan persistence only for an exact artifact or explicit persistence request.

For build/standalone-plan and other optional artifact operations, without an exact artifact or
explicit persistence request commands make no Linear call. A proved new fix is the narrow exception:
diagnosis remains provider-free until root-cause proof, then an exact issue wins or the configured
team creates one issue, with successful write/read-back required. Tracked policy provides validated
defaults only after the relevant selection or fix proof and cannot authorize provider writes.

## Where to go next

<Cards>
  <Card title="Getting started" href="/docs/getting-started" description="Install, initialize local support, and optionally connect Linear artifacts." />
  <Card title="Configuration" href="/docs/configuration" description="Set non-secret repository policy." />
  <Card title="Engineer agents" href="/docs/concepts/engineer-agents" description="Understand decision, coding, review, and acceptance boundaries." />
  <Card title="Core concepts" href="/docs/concepts" description="The build loop, context management, worktrees, and status tracking." />
  <Card title="Harnesses" href="/docs/harnesses" description="Supported hosts, profile isolation, and native capability differences." />
  <Card title="woostack-build" href="/docs/skills/woostack-build" description="Drive a feature from idea to a reviewed PR stack, optionally persisting one explicitly selected plan hierarchy." />
  <Card title="All skills" href="/docs/skills/using-woostack" description="Per-skill reference, generated from each SKILL.md." />
</Cards>
