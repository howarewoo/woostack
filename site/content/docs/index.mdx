---
title: What is woostack?
description: Evidence-driven delivery with local run authority, optional Linear mirror records, and repository truth.
---

woostack is a model-agnostic collection of gated software-development skills for planning,
implementation, review, diagnostics, and reusable knowledge.

```bash
pnpx skills add howarewoo/woostack
```

- **Split canonical truth:** Local runs in `.woostack/tmp/runs/<run-id>/` own build/fix specifications,
  plans, and approval records, with optional Linear mirroring (`linear.saveArtifacts: true`); Git
  and GitHub prove source, branches, pull requests, reviews, and merge state.
- **Simple durable shape:** every build manages one high-level specification plus direct increment
  contracts with dependency relations in its retained local run manifest. In the optional mirror
  flow, these map to one Linear project and direct issues.
- **Exact revision approval:** the responsible user's two active-conversation approval events clear
  only the matching project-specification and execution-plan gate-file hashes; material edits
  invalidate the affected gate.
- **External-engineer boundary:** Hermes is documented at the
  [top-level Hermes guide](/docs/hermes) as an external engineer that may drive one persistent
  OMP session; it is not a supported installed host or coding runtime.
- **Non-authoritative diagnostics:** local reports are evidence, not permission or lifecycle state.
- **Agent- and model-agnostic:** the same skill logic supports
  [multiple harnesses](/docs/harnesses).
- **Stable adoption surface:** one install provides twenty-one public command/adoption skills at
  twenty-three fixed `SKILL.md` locations.
- **Pairs with [impeccable](https://github.com/pbakaus/impeccable):** woostack's recommended
  front-end design skill.

## Why woostack?

The collection keeps the whole delivery loop coherent without requiring an external service:

- one install and one routing surface;
- exact content-bound approval gates and executor-ready task contracts;
- sequential execution within each local run, with safe concurrency across disjoint run IDs;
- external engineers can review harness evidence without receiving implementation authority;
- portable execution across supported coding harnesses;
- canonical local run authority for fix/build with optional Linear mirroring, and optional artifacts elsewhere.

Build and project-backed Fix allocate or resume a canonical local run store under
`.woostack/tmp/runs/<run-id>/` by default. When `linear.saveArtifacts: true`, approved artifacts mirror
to Linear in one bounded cycle; mirror failure is nonblocking. Retained run artifacts enable exact
resume with `/woostack-execute --run <exact-run-id> [--recheck]`. Tracked policy supplies validated
defaults only when Linear mirroring is enabled.

## Where to go next

<Cards>
  <Card title="Getting started" href="/docs/getting-started" description="Install, initialize local support, and configure repository policy." />
  <Card title="Configuration" href="/docs/configuration" description="Set non-secret repository policy." />
  <Card title="Hermes as an external engineer" href="/docs/hermes" description="Drive one persistent OMP session without installing woostack in Hermes." />
  <Card title="Core concepts" href="/docs/concepts" description="The build loop, context management, worktrees, and status tracking." />
  <Card title="Harnesses" href="/docs/harnesses" description="Supported coding harnesses, native capability differences, and OMP workers." />
  <Card title="woostack-build" href="/docs/skills/woostack-build" description="Drive one canonical local project specification and approved increment plan to a reviewed PR stack." />
  <Card title="All skills" href="/docs/skills/using-woostack" description="Per-skill reference, generated from each SKILL.md." />
</Cards>
